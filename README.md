# Virtual_Antropomorphal_guider
# Для коректной работы необходим Visual Studia С++ (При установки выбрать в дополнительно C++). Ссылка на скачивания: https://visualstudio.microsoft.com/ru/visual-cpp-build-tools/
# Последовательность команд в терминале для запусска:
# pip install -r requirements.txt
# pip populate_db.py
# uvicorn main:app --reload --host 0.0.0.0 --port 8000
